#include "gauss.h"

matrix* allocateMatrix(matrix* mat, unsigned int rows, unsigned int cols) {
    // Check that rows and cols are bigger than 0
    if (rows == 0 || cols == 0) {
        return NULL;
    }
}

void initMatrixWithValue(matrix* mat, unsigned int rows, unsigned int cols, double value) {

}